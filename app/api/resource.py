# TODO: Resource Server -
# This would be an API that accepts the access token (JWT) on protected endpoints.
# Either:
# •	auth-service also act as a resource server by having /users, etc. validate the JWT.
# •	Or split it into another service
